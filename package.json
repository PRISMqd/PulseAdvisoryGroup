{
  "name": "pulse-advisory-group-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:browser": "node tests/responsive-browser.cjs",
    "test:all": "npm test && npm run test:browser"
  }
}
